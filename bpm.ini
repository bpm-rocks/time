[package]
name=time
version=1.0.1
include=libtime
description=Timestamps and getting timestamp differences.

[dependencies]
assign=*

[devDependencies]
unittest=*
bench=*

[scripts]
readme=embed-docs README.md libstrict
test=unittest test/*.test
